# ACME account email for Let's Encrypt (expiry/registration notices only,
# not a secret). Must be set by the operator before first sync — see
# docs/runbooks/cert-manager-external-dns.md.
acmeEmail: "justinang177@gmail.com"

cert-manager:
  crds:
    enabled: true
